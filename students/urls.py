from django.urls import path
# from .views import you views functions here

urlpatterns = [

    # Remember the convention of url paths, initial points = index page for the app, this means the first url path you build, the first argument should be empty ie...path('', view_function, name='your_url_name')
    # path(),  # Include app1's URL patterns, these urls map to courses views like we did with the home page.
]